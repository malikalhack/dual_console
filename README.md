## Dual console project (POC).
Proof of concept for a dual console. The task must have one console for communicating with the user, and a second for outputting library data.  
Setting regions in the console does not work well with scaling the console window. In addition, user input cannot be fully controlled.
